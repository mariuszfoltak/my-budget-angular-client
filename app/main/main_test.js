'use strict';

describe('myBudget.main module', function() {

  beforeEach(module('myBudget.main'));

  describe('main controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var mainCtrl = $controller('MainCtrl');
      expect(mainCtrl).toBeDefined();
    }));

  });
});